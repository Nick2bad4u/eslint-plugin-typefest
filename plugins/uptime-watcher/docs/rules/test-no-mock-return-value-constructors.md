# Tests: No mockReturnValue constructors

Rule ID: `uptime-watcher/test-no-mock-return-value-constructors`

## Summary

Disallow `mockReturnValue`/`mockReturnValueOnce` for constructable mocks; require constructable-mock helpers.

## Options

This rule has no options.
