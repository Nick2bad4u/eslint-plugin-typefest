# Electron: No direct ipcMain.handle usage

Rule ID: `uptime-watcher/electron-no-direct-ipc-handle`

## Summary

Disallow direct `ipcMain.handle`/`ipcMain.handleOnce` usage outside the centralized IPC registration helpers.

## Options

This rule has no options.
