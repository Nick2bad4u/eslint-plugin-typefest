# Renderer: No direct preload bridge usage

Rule ID: `uptime-watcher/renderer-no-direct-preload-bridge`

## Summary

Disallow using the preload bridge directly from application code; prefer the renderer service wrappers.

## Options

This rule has no options.
