# Renderer: No browser dialogs

Rule ID: `uptime-watcher/renderer-no-browser-dialogs`

## Summary

Disallow `alert`, `confirm`, and `prompt` in renderer runtime code.

## Options

This rule has no options.
