# Imports: Prefer @shared alias

Rule ID: `uptime-watcher/prefer-shared-alias`

## Summary

Prefer importing shared modules via the `@shared/*` alias instead of deep relative paths.

## Options

This rule has no options.
