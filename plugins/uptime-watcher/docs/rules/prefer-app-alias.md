# Imports: Prefer @app alias

Rule ID: `uptime-watcher/prefer-app-alias`

## Summary

Prefer importing renderer modules via the `@app/*` alias instead of deep relative paths.

## Options

This rule has no options.
