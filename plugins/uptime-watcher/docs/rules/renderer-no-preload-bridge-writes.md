# Renderer: No preload bridge writes

Rule ID: `uptime-watcher/renderer-no-preload-bridge-writes`

## Summary

Disallow mutating the preload bridge API object from the renderer.

## Options

This rule has no options.
