# Errors: No local normalizers

Rule ID: `uptime-watcher/no-local-error-normalizers`

## Summary

Disallow re-declaring `ensureError`/`normalizeError`-style helpers locally; require using the shared error utilities.

## Options

This rule has no options.
