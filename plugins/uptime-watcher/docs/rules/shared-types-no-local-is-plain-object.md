# Shared types: No local isPlainObject

Rule ID: `uptime-watcher/shared-types-no-local-is-plain-object`

## Summary

Prevent local `isPlainObject` helper re-declarations in `shared/types`; require using the shared `typeGuards` helper.

## Options

This rule has no options.
