# Electron: Prefer readProcessEnv()

Rule ID: `uptime-watcher/electron-prefer-read-process-env`

## Summary

Prefer the Electron `readProcessEnv(...)` helper instead of direct `process.env` access.

## Options

This rule has no options.
