/* eslint-disable no-barrel-files/no-barrel-files -- package root entrypoint intentionally forwards to built plugin output */

export { default } from "./dist/plugin.js";

/* eslint-enable no-barrel-files/no-barrel-files */
